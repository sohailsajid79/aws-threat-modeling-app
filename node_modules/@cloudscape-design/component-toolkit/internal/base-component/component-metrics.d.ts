import { ComponentConfiguration, PackageSettings } from './metrics/interfaces';
export { ComponentConfiguration };
export declare function useComponentMetrics(componentName: string, { packageSource, packageVersion, theme }: PackageSettings, configuration?: ComponentConfiguration): void;
